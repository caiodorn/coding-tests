spring:
  profiles:
    active: @spring.profiles.active@
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
server:
  error:
    include-stacktrace: never
    whitelabel:
      enabled: false

